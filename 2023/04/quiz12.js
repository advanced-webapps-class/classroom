// 11.배열값 나누기
const items = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'];
const pageSize = 3;

const result = [];

// result.push(items.slice(0, 3));
// result.push(items.slice(3, 6));
// result.push(items.slice(6, 9));
// result.push(items.slice(9, 12));

// for (let i = 0; i < items.length; i += pageSize) {
//   console.log(i);
// }

console.log(result);
